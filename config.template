# minectl client configuration
# Location: ~/.minectl/config
# Server configs are stored at CONFIG_DIR on the remote host

# Config directory on remote host (required)
# Typically: /home/SSH_USER/minecraft-servers
CONFIG_DIR=/home/minecraft-servers

# SSH user for deployment (used for config directory path)
SSH_USER=minecraft-servers
